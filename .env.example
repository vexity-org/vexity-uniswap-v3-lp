# RPC endpoint for Arbitrum (required)
ARBITRUM_RPC_URL=https://arb-mainnet.g.alchemy.com/v2/<your-key>

# Deployer private key — used as fallback if no --account or --ledger flag is passed
PRIVATE_KEY=0x

# Arbiscan API key — required only for --verify
ETHERSCAN_API_KEY=
