{
  "id": "uniswap-v3-lp:rebalance",
  "name": "Uniswap V3 LP Rebalance",
  "description": "Create or rebalance a Uniswap V3 concentrated liquidity position with a tick range centered on the current pool price. Exits the existing position, collects all fees, computes the optimal token split for the new range, rebalances via a swap, then mints a fresh position centered around the current price.",
  "tags": ["uniswap", "lp", "rebalance", "concentrated-liquidity", "yield", "v3", "position"],
  "triggers": [
    "rebalance uniswap lp position",
    "auto rebalance lp",
    "rebalance concentrated liquidity",
    "keep lp in range",
    "uniswap v3 rebalance",
    "rebalance my uniswap position",
    "auto-compound uniswap lp fees",
    "active lp management",
    "rebalance when out of range",
    "uniswap v3 auto rebalance",
    "re-center my LP",
    "my uniswap LP is out of range",
    "adjust LP range",
    "create a new WETH/USDC LP position"
  ],
  "params": [
    {
      "name": "token0",
      "type": "token",
      "required": true,
      "description": "First token of the LP pair (e.g. WETH). Must have lower address than token1.",
      "validation": "non-empty, must differ from token1"
    },
    {
      "name": "token1",
      "type": "token",
      "required": true,
      "description": "Second token of the LP pair (e.g. USDC). Must have higher address than token0.",
      "validation": "non-empty, must differ from token0"
    },
    {
      "name": "feeTier",
      "type": "number",
      "required": true,
      "description": "Pool fee tier: 100 (0.01%, tickSpacing=1), 500 (0.05%, tickSpacing=10), 3000 (0.3%, tickSpacing=60), or 10000 (1%, tickSpacing=200)",
      "validation": "must be one of: 100, 500, 3000, 10000"
    },
    {
      "name": "rangeWidthBps",
      "type": "number",
      "required": true,
      "description": "Total width of the new range in basis points (e.g. 1000 = ±5% around current price). Converted to tick delta via: tickDelta = floor(ln(1 + halfBps/10000) / ln(1.0001) / tickSpacing) * tickSpacing",
      "validation": "must be between 1 and 20000"
    },
    {
      "name": "driftBufferBps",
      "type": "number",
      "required": false,
      "description": "Optional price drift buffer in bps for automation trigger threshold. Position is rebalanced when price drifts beyond range + buffer.",
      "validation": "if provided, must be positive and less than rangeWidthBps"
    }
  ],
  "steps": [
    {
      "actionId": "uniswap-v3-lp:decrease-liquidity",
      "params": { "tokenId": "$tokenId", "liquidity": "MAX", "amount0Min": "0", "amount1Min": "0" },
      "label": "Remove all liquidity from current position"
    },
    {
      "actionId": "uniswap-v3-lp:collect-fees",
      "params": { "tokenId": "$tokenId", "recipient": "OWNER", "amount0Max": "MAX", "amount1Max": "MAX" },
      "label": "Collect accrued fees and freed liquidity tokens"
    },
    {
      "actionId": "uniswap-v3-lp:calculate-range",
      "params": { "pool": "$pool", "rangeBps": "$rangeWidthBps" },
      "label": "Calculate new tick-aligned range centered on current pool price"
    },
    {
      "actionId": "uniswap-v3-lp:compute-optimal-amounts",
      "params": { "pool": "$pool", "tickLower": "$step.2.tickLower", "tickUpper": "$step.2.tickUpper", "balance0": "$step.1.amount0", "balance1": "$step.1.amount1" },
      "label": "Compute optimal token amounts for the new price range"
    },
    {
      "actionId": "uniswap-v3:swap",
      "params": { "tokenIn": "$step.3.surplusToken", "tokenOut": "$step.3.deficitToken", "amountIn": "$step.3.swapAmount", "fee": "$feeTier" },
      "label": "Swap surplus token to achieve optimal ratio for new range"
    },
    {
      "actionId": "uniswap-v3-lp:mint-position",
      "params": { "token0": "$token0", "token1": "$token1", "fee": "$feeTier", "tickLower": "$step.2.tickLower", "tickUpper": "$step.2.tickUpper", "amount0Desired": "BALANCE", "amount1Desired": "BALANCE", "recipient": "OWNER" },
      "label": "Mint new LP position at the computed tick range"
    }
  ],
  "examples": [
    {
      "input": "Rebalance my WETH/USDC 0.05% LP to a ±5% range",
      "params": {
        "token0": "WETH",
        "token1": "USDC",
        "feeTier": "500",
        "rangeWidthBps": "1000"
      }
    },
    {
      "input": "My WBTC/WETH 0.3% LP is out of range, re-center with 20% range",
      "params": {
        "token0": "WBTC",
        "token1": "WETH",
        "feeTier": "3000",
        "rangeWidthBps": "2000"
      }
    },
    {
      "input": "Create a new WETH/USDC LP position centered on current price with ±2% range",
      "params": {
        "token0": "WETH",
        "token1": "USDC",
        "feeTier": "500",
        "rangeWidthBps": "400"
      }
    },
    {
      "input": "Auto-rebalance my WETH/USDC Uniswap V3 LP position when it goes out of range, ±5% range with a 2% buffer, and compound fees",
      "params": {
        "token0": "WETH",
        "token1": "USDC",
        "feeTier": "500",
        "rangeWidthBps": "1000",
        "driftBufferBps": "200"
      }
    }
  ],
  "conditionTemplate": {
    "actionId": "uniswap-v3-lp:check-rebalance",
    "params": {
      "positionManager": "POSITION_MANAGER",
      "tokenId": "$tokenId",
      "bufferBps": "$driftBufferBps"
    },
    "description": "Triggers rebalance when the position price moves outside the configured range plus the buffer zone. Uses shouldRebalance(positionManager, tokenId, bufferBps) from the UniswapV3LPHelper contract."
  },
  "chains": [1, 42161, 10, 8453]
}
